import type { ReactNode } from "react";

export const metadata = { title: "Fair Intel" };

export default function RootLayout({ children }: { children: ReactNode }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
